<h1>Listing Settings</h1>

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Value</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for setting <- @settings do %>
    <tr>
      <td><%= setting.key %></td>
      <td><%= setting.value %></td>

      <td>
        <%= link "Show", to: Routes.setting_path(@conn, :show, setting) %>
        <%= link "Edit", to: Routes.setting_path(@conn, :edit, setting) %>
        <%= link "Delete", to: Routes.setting_path(@conn, :delete, setting), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Setting", to: Routes.setting_path(@conn, :new) %></span>

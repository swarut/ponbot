<h1>Edit Expense tag</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.expense_tag_path(@conn, :update, @expense_tag)) %>

<span><%= link "Back", to: Routes.expense_tag_path(@conn, :index) %></span>

const defaultState = {
  name: 'taiko'
}

const reducer = (state = defaultState, action) => {
  let arr, min, max, med;
  switch(action.type) {
    case 'a':
      return state

    default:
      return state
  }
}

export default reducer